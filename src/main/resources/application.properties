spring.application.name=Shakwa

# Server Configuration
server.port=13000

# Database Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/mithaq
spring.datasource.username=postgres
spring.datasource.password=password
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
# spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
# spring.jpa.properties.hibernate.format_sql=true

# Flyway Configuration
# Disabled until migration files are created
spring.flyway.enabled=false

# JWT Configuration
jwtKey=7402bb3c24c35f15d1a7f1422078d9c1a4d9ebf1a276ff01ac84e6407625532e

# Mail Configuration
# For Gmail, you need to:
# 1. Enable 2-Step Verification on your Google account
# 2. Generate an App Password: https://myaccount.google.com/apppasswords
# 3. Use the generated App Password (not your regular Gmail password)
# 
# Development Mode: Set mail.enabled=false to log OTP instead of sending email
# Production Mode: Set mail.enabled=true to send OTP via email
mail.enabled=true
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=raneemmahmoud333@gmail.com
spring.mail.password=hsif aevy gxjj dauy
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

## Swagger/OpenAPI Configuration
## Ngrok URL Configuration (update this with your ngrok URL when running)
## Example: https://abc123.ngrok-free.app
#ngrok.url=https://eb1a2fa4db08.ngrok-free.app
## المسار الافتراضي لمواصفة OpenAPI 3.0
#springdoc.api-docs.path=/v3/api-docs
## المسار الافتراضي لـ Swagger UI (base path only)
#springdoc.swagger-ui.path=/swagger-ui
## عنوان Swagger UI
#springdoc.swagger-ui.operationsSorter=method
#springdoc.swagger-ui.tagsSorter=alpha
## تفعيل/تعطيل Swagger UI
#springdoc.swagger-ui.enabled=true
## إعدادات إضافية لتحسين الاستقرار
#springdoc.show-actuator=false
#springdoc.default-produces-media-type=application/json
#springdoc.default-consumes-media-type=application/json
#springdoc.writer-with-default-pretty-printer=true
#springdoc.model-and-view-allowed=false
spring.devtools.restart.enabled=false

# Actuator Configuration (for Metrics)
management.endpoints.web.exposure.include=health,info,prometheus,metrics
management.endpoint.health.show-details=when-authorized
management.metrics.export.prometheus.enabled=true
management.metrics.tags.application=${spring.application.name}

# Firebase Configuration
# Path to Firebase service account JSON file (relative to classpath or absolute path)
# Place your firebase-service-account.json file in src/main/resources/
firebase.service-account.path=mithaq-956c5-firebase-adminsdk-fbsvc-ce3e6736f9.json
# Or use environment variable: FIREBASE_SERVICE_ACCOUNT
firebase.service-account.env=FIREBASE_SERVICE_ACCOUNT
